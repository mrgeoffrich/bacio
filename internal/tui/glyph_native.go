//go:build !(js && wasm)
// +build !js !wasm

package tui

// Nerd Font / Powerline git-branch glyph (U+E0A0). Most developer
// terminals carry a Nerd-patched font, so this renders cleanly.
const repoGlyph = ""
